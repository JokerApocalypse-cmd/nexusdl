# ==============================================================================
#  NexusDL — requirements-all.txt (Tous les extras)
# ==============================================================================
#  Installation complète : core + web + gui + pdf + cbr + convert +
#  flaresolverr + socks + notify + sync.
#
#  Généré depuis pyproject.toml via :
#      uv pip compile pyproject.toml --extra all -o requirements-all.txt
#
#  Pour tout installer d'un coup :
#      pip install -r requirements-all.txt
# ==============================================================================

# --- Core ---
-r requirements.txt

# --- Web ---
-r requirements-web.txt

# --- GUI ---
-r requirements-gui.txt

# --- PDF avancé ---
pypdf==5.1.0
reportlab==4.2.5
chardet==5.2.0
freetype-py==2.5.1

# --- CBR (RAR) ---
rarfile==4.2

# --- Conversion images ---
pillow-avif-plugin==1.4.6 ; python_version < "3.13"
pillow-heif==0.21.0

# --- Cloudflare (FlareSolverr) ---
aiodns==3.2.0
brotli==1.1.0
pycares==4.5.0

# --- SOCKS5 ---
httpx-socks[asyncio]==0.9.2
python-socks[asyncio]==2.5.3
async-timeout==5.0.1

# --- Notifications ---
apprise==1.9.1
requests==2.32.3
requests-oauthlib==2.0.0
oauthlib==3.2.2
PySocks==1.7.1
markdown==3.7
click==8.1.8
certifi==2024.12.14

# --- Synchronisation cloud ---
webdavclient3==3.14.6
boto3==1.35.85
botocore==1.35.85
s3transfer==0.10.4
jmespath==1.0.1
python-dateutil==2.9.0.post0
urllib3==2.3.0
xmltodict==0.14.2
